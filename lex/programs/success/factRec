program factRec;

func int factorial(int n) {
  var {
    int retorno;
  }
  if (n <= 1) {
    retorno = 1;
  } else {
    retorno = n*factorial(n-1);
  }
  return retorno;
}

main {
  var {
    int n;
  }
  n = 10;
  print(factorial(n), '\n');
}
