program fiborec;

func int fibonacci(int n){
    var {
        int retorno;
    }
    retorno = fibonacci(n-2) + fibonacci(n-1);
    return retorno;
}

main {
    var{
        int n, fibo;
    }
    n = 5;
    fibo = fibonacci(n);
}
